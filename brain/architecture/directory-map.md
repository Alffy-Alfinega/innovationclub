# Directory Map

```
innovationclub/
  AGENTS.md              # OpenCode agent instructions
  brain/                 # Project knowledge base (this folder)
  public/                # Static assets (images, fonts, etc.)
  src/
    app/
      favicon.ico        # Browser tab icon
      globals.css        # Global styles
      layout.tsx         # Root layout (fonts, metadata, HTML shell)
      page.tsx           # Home page — Engine + Scene entrypoint
      Content.tsx        # Application content (3D scene + HTML overlay)
  babel.config.js        # Required for Reactylon JSX transform
  eslint.config.mjs      # ESLint 9 flat config
  next.config.ts         # Next.js configuration
  next-env.d.ts          # Next.js TypeScript declarations
  package.json           # Dependencies and scripts
  package-lock.json      # Lockfile
  tsconfig.json          # TypeScript configuration
  .gitignore             # Git ignore rules
  .gitattributes         # Git attributes
  LICENSE                # Apache 2.0
  README.md              # Project readme
```
