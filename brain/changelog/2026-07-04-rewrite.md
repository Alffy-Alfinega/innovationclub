# 2026-07-04 — Clean-slate Next.js rewrite (scaffold complete)

Wiped everything except `.git` and `brain/` per Nashif's instruction.

## Built & verified (production build passes, 0 type errors, 0 lint errors)
- Next.js 16 scaffold, TS 6, Tailwind 4
- Prisma 7 schema: School (tenant), User (5 roles incl. BREAK_GLASS),
  Student, ParentStudent join, AuditLog
- Auth.js v5 credentials auth; role + schoolId embedded in JWT/session;
  break-glass logins auto-audited
- src/proxy.ts: single role+scope gate for all /dashboard/* routes
- Dashboards: super (cross-school), school (scoped), mentor (club members),
  parent (linked children), denied
- /register with Server Action (structural CSRF), same validation rules
  as the Express patch
- Isolated Babylon.js 3D layer (marketing only, lazy, Data-Saver fallback)

## Not yet done
- prisma migrate against the real Neon DB (needs real DATABASE_URL)
- Seed script (first school + first SUPER_ADMIN)
- Data migration from old `students` table to new schema
- Vercel deploy
- Real Alfinega 3D brand asset (placeholder icosphere in place)
- Vascular Earth showcase page rebuild
